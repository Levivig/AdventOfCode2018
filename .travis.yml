dist: trusty

jobs:
  include:
    - stage: build docker image
      script:
      - docker login -u $DOCKER_USER -p $DOCKER_PASS
      - docker build --no-cache -t levivig/adventofcode . 
      - docker images
      - docker push $DOCKER_USERNAME/adventofcode
